/**
 * POC defaults — single source of truth for frontend POC constants.
 * Import from this file; never hardcode these values inline.
 */
export const DEFAULT_ORG_ID = 'aaaaaaaaaaaaaaaaaaaaaaaa';
export const DEFAULT_USER_ID = 'admin-user';
